
import { highlighter } from 'keyword-highlighter/helpers/highlighter';
import { module, test } from 'qunit';

module('Unit | Helper | highlighter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = highlighter([42]);
  assert.ok(result);
});

